RSpec.describe ActiveRecord::NestedAttributes::DestroyIf do
  it "has a version number" do
    expect(ActiveRecord::NestedAttributes::DestroyIf::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
